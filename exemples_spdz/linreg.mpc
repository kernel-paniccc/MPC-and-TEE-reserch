X = sfix.input_tensor_via(0, [[1, 2, 3], [11, 12, 13]]) # участник 0 передает матрицу признаков
y = sint.input_tensor_via(1, [0, 1]) # участник 1 передает вектор меток

#типичный пример FL с тензором одного участника и данными другого

from Compiler import ml 
log = ml.SGDLogistic(100) # логистическая регрессия со стохастическим градиентным спуском (100 эпох)
log.fit(X, y) # выполняются итерации градиентного спуска, обновляющие веса модели, все операции с данными происходят в зашифрованном виде

print_ln('%s', log.predict(X).reveal())
