enclave { 
	trusted { 
		public void ecall_process( 
			[in, size=len] const uint8_t* input, 
			size_t len, 
			[out, size=out_len] uint8_t* output, 
			size_t out_len);
 		}; 
	}; 
	untrusted { 
		void ocall_log([in, string] const char* message);
	};
};
